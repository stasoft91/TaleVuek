export function text (defaultValue) {
  return {
    type: 'text',
    defaultValue,
    isTalevuekKnob: true
  }
}

export function boolean (defaultValue) {
  return {
    type: 'boolean',
    defaultValue,
    isTalevuekKnob: true
  }
}
